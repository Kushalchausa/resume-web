/** @type {import('next').NextConfig} */
const nextConfig = {
    // pdf2json is a node module, usually doesn't need this, but good to keep if we see issues. 
    // transforming it to empty or simple config for now.
};

export default nextConfig;
